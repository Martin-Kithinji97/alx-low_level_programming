#include <stdio.h>

/**
  *main - prints out the sentense
  *
  *Return: Always 0 if command is succesful
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
