#include "main.h"
/**
  * mul - function multiplies integers
  * @a: the parameter takes in an integer
  * @b: the parameter takes in an integer
  * Return: it returns muliplication of 2 integers
  */

int mul(int a, int b)
{
	return (a * b);
}
